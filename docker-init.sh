#!/bin/sh

# Create data directory if it doesn't exist
mkdir -p /data

# Initialize IndexedDB data if needed (placeholder for now)
# Since IndexedDB is browser-based, we'll handle this client-side

# Start nginx
nginx -g "daemon off;"